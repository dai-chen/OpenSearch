# Codebase Patterns
(Patterns discovered during implementation will be consolidated here)

## Known Patterns from Design Phase
- OpenSearch modules live in `modules/<name>/` with `build.gradle` using `opensearchplugin {}` block
- Modules are registered in root `settings.gradle` via `addSubProjects` or similar
- REST handlers extend `BaseRestHandler`, override `routes()` and `prepareRequest()`
- Plugin classes implement `ActionPlugin` and register handlers via `getRestHandlers()`
- Reference modules: `modules/reindex/`, `modules/lang-painless/` for build.gradle patterns
- The unified-query-api Maven coordinates: `org.opensearch.query:unified-query-api` (check `sandbox/plugins/analytics-engine/build.gradle` for exact repo URL and version)
- The IT pattern for unified query: `UnifiedQueryPlanner.plan()` → `UnifiedQueryCompiler.compile()` → `PreparedStatement.executeQuery()` → `ResultSet`
- Schema is built as a lazy `AbstractSchema` that creates `OpenSearchIndex(osClient, settings, indexName)` on demand
- Use `OpenSearchNodeClient` (not REST client) since the module runs in-process
- OpenSearch jar hell checker will reject duplicate classes — resolve via exclusions and resolutionStrategy.force
- Integration tests go in `src/internalClusterTest/java/`, class names end with "IT", extend `OpenSearchIntegTestCase`

## Discovered Patterns (US-001)
- `settings.gradle` auto-discovers modules — just create `build.gradle` in `modules/<name>/`, no settings.gradle edit needed

## Discovered Patterns (US-002)
- `unified-query-opensearch` jar (org.opensearch.query:unified-query-opensearch) contains the full opensearch module: OpenSearchIndex, OpenSearchNodeClient, CalciteLogicalIndexScan, OpenSearchDataType, etc.
- No need to copy source files from the sql repo — just add the jar as a dependency with appropriate exclusions
- Constructor pattern: `new OpenSearchIndex(new OpenSearchNodeClient(nodeClient), settings, indexName)`
- Schema pattern: lazy `AbstractSchema` with `getTableMap()` returning `OpenSearchIndex` on demand, passed to `UnifiedQueryContext.builder().catalog(name, schema)`
- Guava forbidden on compile classpaths — use custom `calciteCompile` configuration, add to sourceSets manually
- Per-dependency exclusions for server-provided deps (jackson, log4j, slf4j, etc.) — NOT global `configurations.all` excludes (breaks test framework)
- `forbiddenApisMain` needs all referenced classes on compile classpath — exclude server deps from runtime only, not compile
- `thirdPartyAudit.ignoreMissingClasses()` for server-provided classes, `ignoreViolations()` for guava Unsafe
- `dependencyLicenses { mapping from: /pattern/, to: 'name' }` groups jars under shared LICENSE/NOTICE
- `./gradlew :modules:query-languages:updateShas` auto-generates `.jar.sha1` files
- `testingConventions` requires at least one test in both `test` and `internalClusterTest` source sets
- All public classes/methods need javadoc with `@param` tags (enforced by `missingJavadoc`)
- Package needs `package-info.java` with javadoc

## Discovered Patterns (US-003)
- Action names MUST use valid prefixes: `indices:data/read/`, `indices:data/write/`, `cluster:admin/`, `cluster:monitor/`, `indices:admin/`, `indices:monitor/`, `internal:`, `indices:internal/`, `cluster:internal/`, `views:` — anything else (e.g. `cluster:data/`) is rejected by the test framework
- Transport action is reusable for SQL and PPL — differentiated by `QueryType` enum in the request
- `HardcodedSettings` extends `org.opensearch.sql.common.setting.Settings` for PoC defaults
- Constructor javadoc required even for no-arg constructors on public classes

## GUARDRAILS — DO NOT VIOLATE
- ONLY modify files inside `modules/query-languages/` (exception: `settings.gradle` for module registration in US-001 only)
- Do NOT modify any existing module, plugin, or server code
- Do NOT modify `sandbox/plugins/analytics-engine/` — it is unrelated to this work
- Do NOT add dependencies to the root build.gradle or any other module's build.gradle
- Do NOT delete or modify any existing tests
- Keep the implementation minimal — this is a PoC, not production code
- Use hardcoded default settings, not configurable plugin settings
- No explain support, no cursor/pagination, no async query
- If jar conflicts arise, resolve via exclusions/force in THIS module's build.gradle only

# Iteration Log

<!-- Round summaries and human intervention notes will be appended here automatically -->

## 2026-03-21T01:15 - US-001
- Created `modules/query-languages/` module skeleton with build.gradle, plugin class, and tests
- Files changed:
  - `modules/query-languages/build.gradle` — opensearchplugin config, unified-query-api/core/ppl deps, calcite, guava, janino
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/QueryLanguagesPlugin.java` — Plugin + ActionPlugin with empty getRestHandlers/getActions
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/package-info.java`
  - `modules/query-languages/src/test/java/org/opensearch/querylanguages/QueryLanguagesPluginTests.java` — unit test
  - `modules/query-languages/src/internalClusterTest/java/org/opensearch/querylanguages/QueryLanguagesPluginIT.java` — integration test
  - `modules/query-languages/licenses/` — LICENSE, NOTICE, and SHA1 files for all bundled deps
- **Learnings for future iterations:**
  - `settings.gradle` auto-discovers modules via `addSubProjects` scanning for `build.gradle` — no need to modify it
  - Guava is forbidden on compile/test compile classpaths by `gradle/forbidden-dependencies.gradle` — use a custom `calciteCompile` configuration and add to sourceSets manually
  - Exclusions for server-provided deps (jackson, log4j, slf4j, etc.) must be per-dependency, NOT global `configurations.all` — global exclusions break test framework which also needs those deps
  - `forbiddenApisMain` task needs all referenced classes on compile classpath (log4j, joda-time, etc.) — don't exclude from compile, only from runtime via per-dependency excludes
  - `thirdPartyAudit` requires `ignoreMissingClasses` for server-provided classes and `ignoreViolations` for guava Unsafe usage
  - `dependencyLicenses` mapping groups multiple jars under one LICENSE/NOTICE (e.g., `mapping from: /unified-query-.*/, to: 'unified-query'`)
  - `updateShas` task auto-generates `.jar.sha1` files
  - `testingConventions` requires at least one test class in both `test` and `internalClusterTest` source sets
  - All public classes and methods need javadoc with `@param` tags — enforced by `missingJavadoc` task
  - Package needs `package-info.java` with javadoc
---

## 2026-03-21T01:20 - US-002
- Added `unified-query-opensearch` jar as dependency instead of copying source files
- This jar (org.opensearch.query:unified-query-opensearch:3.6.0.0-SNAPSHOT) already contains:
  - OpenSearchIndex (Calcite Table implementation extending AbstractOpenSearchTable)
  - OpenSearchClient interface and OpenSearchNodeClient implementation
  - CalciteLogicalIndexScan, AbstractCalciteIndexScan (for toRel())
  - OpenSearchDataType, IndexMapping, OpenSearchDescribeIndexRequest (for getFieldTypes())
  - OpenSearchRequestBuilder, OpenSearchResponse, scan operators, pushdown rules
  - No DataSourceService/StorageEngine coupling (verified via javap)
- Files changed:
  - `modules/query-languages/build.gradle` — added unified-query-opensearch dependency with exclusions
  - `modules/query-languages/licenses/unified-query-opensearch-3.6.0.0-SNAPSHOT.jar.sha1` — SHA for new jar
- All checks pass: build, test, internalClusterTest, jarHell, thirdPartyAudit, dependencyLicenses
- **Learnings for future iterations:**
  - The `unified-query-opensearch` jar exists at `org.opensearch.query:unified-query-opensearch` and contains the entire opensearch module from the sql repo — no need to copy source files
  - It depends on `org.opensearch:opensearch` (server) which must be excluded since it's provided by the module framework
  - It also has runtime deps on `opensearch-ml-client`, `geospatial-client`, `resilience4j-retry`, `org.json:json` — all excluded since they're not needed for the PoC
  - The existing `unified-query` license/notice mapping (`mapping from: /unified-query-.*/, to: 'unified-query'`) automatically covers the new jar
  - For US-003/004: Use `OpenSearchIndex(new OpenSearchNodeClient(nodeClient), settings, indexName)` to create tables, and pass a lazy AbstractSchema to `UnifiedQueryContext.builder().catalog(name, schema)`
---

## 2026-03-21T01:35 - US-003
- Implemented POST /_ppl REST endpoint with transport action and unified query pipeline
- Files changed:
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/QueryLanguageAction.java` — ActionType singleton with name `indices:data/read/query_language`
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/QueryLanguageRequest.java` — ActionRequest with query string and QueryType
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/QueryLanguageResponse.java` — ActionResponse with schema/datarows JSON format, implements ToXContentObject
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/TransportQueryLanguageAction.java` — HandledTransportAction orchestrating: OpenSearchNodeClient → lazy AbstractSchema → UnifiedQueryContext → UnifiedQueryPlanner.plan() → UnifiedQueryCompiler.compile() → PreparedStatement.executeQuery() → ResultSet → response
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/RestPplAction.java` — BaseRestHandler for POST /_ppl, parses JSON body with 'query' field, delegates to transport action
  - `modules/query-languages/src/main/java/org/opensearch/querylanguages/QueryLanguagesPlugin.java` — Updated to register RestPplAction and TransportQueryLanguageAction
  - `modules/query-languages/src/test/java/org/opensearch/querylanguages/QueryLanguagesPluginTests.java` — Updated assertions for 1 handler and 1 action
- **Learnings for future iterations:**
  - Action names MUST start with a valid prefix: `cluster:admin`, `indices:data/read`, `indices:data/write`, `indices:monitor`, `indices:admin`, `cluster:monitor`, `cluster:internal`, `internal:`, `indices:internal`, `views:` — `cluster:data/...` is NOT valid
  - The transport action is reusable for both SQL and PPL — just pass different `QueryType` enum value
  - `HardcodedSettings` extends `org.opensearch.sql.common.setting.Settings` (not OpenSearch's Settings) — provides PoC defaults for CALCITE_ENGINE_ENABLED, CALCITE_PUSHDOWN_ENABLED, QUERY_SIZE_LIMIT, etc.
  - `UnifiedQueryContext.Builder.setting(key, value)` accepts string keys for additional settings beyond what the Settings object provides
  - Schema is built lazily via `AbstractSchema.getTableMap()` — iterates `osClient.indices()` and creates `OpenSearchIndex(osClient, settings, indexName)` for each
  - `RestToXContentListener` handles serializing `ToXContentObject` responses to the REST channel
  - Constructor javadoc is required even for no-arg constructors on public classes
---
